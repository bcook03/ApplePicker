using UnityEngine;

public class RoundConrol : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public int round = 1;
    public int maxRounds = 4;
    
}
